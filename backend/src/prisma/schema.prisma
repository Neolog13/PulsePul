generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Measurement {
    id    String @id @default(uuid())
    date  String
    time  String
    sap   String
    dap   String
    pulse String
}
